coverage:
  status:
    project:
      default:
        target: 95%
        threshold: 0%
        informational: false
    patch:
      default:
        target: 95%
        threshold: 0%
        informational: false

ignore:
  # Test utility components are not production code and don't need branch coverage
  - 'src/lib/components/testing/**'
